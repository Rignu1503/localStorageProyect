package com.rigo.local.storage.localStorageProyect.utils.enums;

public enum PurchaseStatus {

    PENDING,
    APPROVED,
    SHIPPED,
    RECEIVED,
    CANCELLED,

    /*
     * PENDING -> Pendiente
     * APPROVED → Aprobada.
     * SHIPPED → Enviada.
     * RECEIVED → Recibida.
     * Cancelled  → Cancelada.
     * */

}
